spring:
    application:
        name: backend
    datasource:
        url: jdbc:postgresql://108.142.2.235:5432/gvigaidb?currentSchema=public
        username: gvigaiuser
        password: daf34kYmrj
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: validate
        show-sql: false
        open-in-view: false
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
    # liquibase: # for future use in the project
    # change-log: classpath:db/changelog-master.xml
    # contexts: test
    # test-rollback-on-update: true
